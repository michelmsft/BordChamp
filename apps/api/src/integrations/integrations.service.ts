import{authorize}from"@bordchamp/authz";import type{ActorContext}from"@bordchamp/domain";import{createHmac,randomUUID}from"node:crypto";import{lookup}from"node:dns/promises";import{BadRequestException,ConflictException,ForbiddenException,Inject,Injectable,NotFoundException}from"@nestjs/common";import{INTEGRATION_REPOSITORY,IntegrationConflictError,type DeliveryAttempt,type EventTopic,type IntegrationEvent,type IntegrationRepository}from"./integration.repository.js";import type{TableEntity}from"@azure/data-tables";import{createTableClient,hasTableStorageConfiguration}from"../storage/table-client.js";
const TOPICS:readonly EventTopic[]=["public.market.snapshot","private.trade.executed","private.settlement.updated","private.delivery.updated","private.risk.alert"];
type AttemptChanges=Omit<Partial<Omit<DeliveryAttempt,"etag">>,"lastError"|"lastHttpStatus">&{lastError?:string|undefined;lastHttpStatus?:number|undefined};
@Injectable()export class IntegrationsService{constructor(@Inject(INTEGRATION_REPOSITORY)private repo:IntegrationRepository){}
 async createSubscription(actor:ActorContext,endpoint:string,topics:readonly string[]){await validateEndpoint(endpoint);const selected=[...new Set(topics)]as EventTopic[];if(selected.length===0||!selected.every(t=>TOPICS.includes(t)))throw new BadRequestException("Invalid webhook topic");const privateTopic=selected.some(t=>t.startsWith("private."));const organizationId=actor.organizationId;if(privateTopic&&organizationId===undefined)throw new BadRequestException("Private topics require an organization");const action=privateTopic?"webhook.subscription.private.manage":"webhook.subscription.public.manage";const result=authorize({action,actor,entityStatus:"active",...(organizationId===undefined?{}:{resourceOrganizationId:organizationId})});if(!result.allowed)throw new ForbiddenException(result.reason);const ownerScope=organizationId===undefined?`USER:${actor.userId}`:`ORG:${organizationId}`;const id=randomUUID();const now=new Date().toISOString();const value=await this.map(()=>this.repo.createSubscription({id,ownerScope,...(organizationId===undefined?{}:{organizationId}),userId:actor.userId,endpoint,topics:selected,status:"active",createdAt:now,updatedAt:now}));return{subscription:value,signingSecret:deriveSecret(id,ownerScope)};}
 async listSubscriptions(actor:ActorContext){const scope=scopeFor(actor);return this.repo.listSubscriptions(scope);}
 async publish(topic:EventTopic,organizationId:string|undefined,subjectId:string,payload:Record<string,unknown>){if(!TOPICS.includes(topic))throw new BadRequestException("Invalid event topic");if(topic.startsWith("private.")&&organizationId===undefined)throw new Error("Private event requires organization");const occurredAt=new Date().toISOString();const sequenceId=`${String(Date.now()).padStart(13,"0")}-${randomUUID()}`;const event:IntegrationEvent={id:randomUUID(),sequenceId,topic,visibility:topic.startsWith("public.")?"public":"private",...(organizationId===undefined?{}:{organizationId}),subjectId,payload,occurredAt};await this.repo.saveEvent(event);const subscriptions=await this.repo.listMatching(topic,organizationId);for(const sub of subscriptions){const body=JSON.stringify(event);const signature=sign(sub.id,sub.ownerScope,event.id,sequenceId,body);const now=new Date().toISOString();await this.repo.createAttempt({id:`${sub.id}:${event.id}`,subscriptionId:sub.id,ownerScope:sub.ownerScope,eventId:event.id,sequenceId,topic,endpoint:sub.endpoint,payloadJson:body,signature,status:"pending",attemptCount:0,replayCount:0,nextAttemptAt:now,updatedAt:now});}return event;}
 async publishBestEffort(topic:EventTopic,organizationIds:readonly string[],subjectId:string,payload:Record<string,unknown>){for(const org of new Set(organizationIds)){try{await this.publish(topic,org,subjectId,payload);}catch{}}}
 async publicFeed(after:string|undefined,limit:number){if(!Number.isSafeInteger(limit)||limit<1||limit>100)throw new BadRequestException("limit must be 1-100");return this.repo.listPublicEvents(after,limit);}
 async privateFeed(actor:ActorContext,after:string|undefined,limit:number){if(!Number.isSafeInteger(limit)||limit<1||limit>100)throw new BadRequestException("limit must be 1-100");const org=actor.organizationId;if(org===undefined)throw new BadRequestException("Private feed requires an organization");const result=authorize({action:"webhook.subscription.private.manage",actor,entityStatus:"active",resourceOrganizationId:org});if(!result.allowed)throw new ForbiddenException(result.reason);if(!hasTableStorageConfiguration()){const memory=this.repo as IntegrationRepository&{listPrivateEvents?:(o:string,a:string|undefined,l:number)=>Promise<readonly IntegrationEvent[]>};return memory.listPrivateEvents?.(org,after,limit)??[];}const out:IntegrationEvent[]=[];for await(const raw of createTableClient(process.env.AZURE_STORAGE_INTEGRATIONS_TABLE??"Integrations").listEntities({queryOptions:{filter:`PartitionKey eq 'EVENTS:ORG:${org}'${after===undefined?"":` and RowKey gt '${after}'`}`}})){const e=raw as TableEntity&Record<string,unknown>;out.push({id:e.id as string,sequenceId:e.rowKey,topic:e.topic as EventTopic,visibility:"private",organizationId:org,subjectId:e.subjectId as string,payload:JSON.parse(e.payloadJson as string)as Record<string,unknown>,occurredAt:e.occurredAt as string});if(out.length>=limit)break;}return out;}
 async listAttempts(actor:ActorContext){return this.repo.listAttempts(scopeFor(actor));}
 async dispatch(actor:ActorContext,id:string){const attempt=await this.requireOwnedAttempt(actor,id);if(attempt.status==="succeeded")return attempt;if(new Date(attempt.nextAttemptAt).getTime()>Date.now())throw new BadRequestException("Attempt is not due yet");await validateEndpoint(attempt.endpoint);let statusCode:number|undefined;let error:string|undefined;try{const response=await fetch(attempt.endpoint,{method:"POST",headers:{"content-type":"application/json","x-bordchamp-event-id":attempt.eventId,"x-bordchamp-sequence":attempt.sequenceId,"x-bordchamp-signature":attempt.signature},body:attempt.payloadJson,signal:AbortSignal.timeout(10_000),redirect:"error"});statusCode=response.status;if(response.ok)return this.update(attempt,{status:"succeeded",attemptCount:attempt.attemptCount+1,lastHttpStatus:statusCode,lastError:undefined,nextAttemptAt:new Date().toISOString()});error=`HTTP ${response.status}`;}catch(e){error=e instanceof Error?e.message:"Webhook delivery failed";}const count=attempt.attemptCount+1;const dead=count>=5;return this.update(attempt,{status:dead?"deadLetter":"retryScheduled",attemptCount:count,...(statusCode===undefined?{}:{lastHttpStatus:statusCode}),lastError:error,nextAttemptAt:new Date(Date.now()+Math.min(300_000,2**count*1000)).toISOString()});}
 async replay(actor:ActorContext,id:string){const a=await this.requireOwnedAttempt(actor,id);return this.update(a,{status:"pending",replayCount:a.replayCount+1,nextAttemptAt:new Date().toISOString(),lastError:undefined});}
 private async requireOwnedAttempt(actor:ActorContext,id:string){const a=await this.repo.getAttempt(id);if(!a)throw new NotFoundException("Webhook attempt not found");const org=actor.organizationId;const result=authorize({action:"webhook.attempt.replay",actor,entityStatus:a.status==="deadLetter"?"suspended":"active",...(org===undefined?{}:{resourceOrganizationId:org})});if(!result.allowed||a.ownerScope!==scopeFor(actor))throw new ForbiddenException("Attempt is outside actor scope");return a;}
 private update(a:DeliveryAttempt,changes:AttemptChanges){const{etag,...base}=a;const merged={...base,...changes,updatedAt:new Date().toISOString()};const next:Omit<DeliveryAttempt,"etag">={id:merged.id,subscriptionId:merged.subscriptionId,ownerScope:merged.ownerScope,eventId:merged.eventId,sequenceId:merged.sequenceId,topic:merged.topic,endpoint:merged.endpoint,payloadJson:merged.payloadJson,signature:merged.signature,status:merged.status,attemptCount:merged.attemptCount,replayCount:merged.replayCount,nextAttemptAt:merged.nextAttemptAt,updatedAt:merged.updatedAt,...(merged.lastHttpStatus===undefined?{}:{lastHttpStatus:merged.lastHttpStatus}),...(merged.lastError===undefined?{}:{lastError:merged.lastError})};return this.map(()=>this.repo.updateAttempt(next,etag));}
 private async map<T>(op:()=>Promise<T>){try{return await op();}catch(e){if(e instanceof IntegrationConflictError)throw new ConflictException("Integration state changed; refresh and retry");throw e;}}}
function scopeFor(actor:ActorContext){return actor.organizationId===undefined?`USER:${actor.userId}`:`ORG:${actor.organizationId}`;}function master(){const s=process.env.WEBHOOK_SIGNING_MASTER_SECRET;if(s===undefined||s.length<24)throw new Error("WEBHOOK_SIGNING_MASTER_SECRET must be at least 24 characters");return s;}function deriveSecret(id:string,scope:string){return createHmac("sha256",master()).update(`${scope}:${id}`).digest("hex");}function sign(id:string,scope:string,eventId:string,sequence:string,body:string){return createHmac("sha256",deriveSecret(id,scope)).update(`${eventId}.${sequence}.${body}`).digest("hex");}
async function validateEndpoint(value:string){let url:URL;try{url=new URL(value);}catch{throw new BadRequestException("Invalid webhook endpoint");}if(url.protocol!=="https:")throw new BadRequestException("Webhook endpoint must use HTTPS");const h=url.hostname.toLowerCase().replace(/^\[|\]$/g,"");if(h==="localhost"||h.endsWith(".local")||isPrivateAddress(h))throw new BadRequestException("Private webhook endpoints are not allowed");if(process.env.NODE_ENV!=="test"){let addresses:{address:string}[];try{addresses=await lookup(h,{all:true,verbatim:true});}catch{throw new BadRequestException("Webhook endpoint hostname cannot be resolved");}if(addresses.some(a=>isPrivateAddress(a.address)))throw new BadRequestException("Webhook endpoint resolves to a private address");}}
function isPrivateAddress(h:string){const m=/^(\d+)\.(\d+)\.(\d+)\.(\d+)$/.exec(h);if(m!==null)return Number(m[1])===10||Number(m[1])===127||Number(m[1])===0||Number(m[1])===169&&Number(m[2])===254||Number(m[1])===192&&Number(m[2])===168||Number(m[1])===172&&Number(m[2])>=16&&Number(m[2])<=31;const x=h.toLowerCase();return x==="::1"||x==="::"||x.startsWith("fc")||x.startsWith("fd")||x.startsWith("fe80:");}